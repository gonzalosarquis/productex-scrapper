/**
 * Integración Apify — placeholder (fase posterior).
 * Aquí se iniciarán runs del actor y se mapearán resultados.
 */

export async function startApifyRunPlaceholder(): Promise<{ runId: string }> {
  return { runId: 'placeholder' }
}
