/**
 * Funciones de análisis — placeholder (fase posterior).
 */

export function computeWholesalePotentialScorePlaceholder(
  followers: number
): number {
  return Math.min(100, Math.floor(followers / 1000))
}
